<!DOCTYPE html>
<html lang='en'>

  <head>
    <meta charset='utf-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <title>Hobbista</title>
    <link rel='stylesheet' type='text/css' href='/css/style.css' />
  </head>

  <body>
    <header>
      <h1>Hobbista</h1>
      <h3>We'll pour in your interests, stir in some hobbies and finish it with
        some new leads!</h3>
    </header>
    <nav>
      <a href='/'>Home</a>
      <!-- If the user is logged in, show a logout button -->
      {{#if loggedIn}}
      <a href="/dashboard">Dashboard</a>
        <button id='logout' class='btn-no-style'>Logout</button>
      {{else}}
        <!-- Otherwise, show a link to login -->
        <a href='/login'>Login</a>
      {{/if}}
    </nav>
    <main>
      <aside>
        <h2>Enjoy the threads and have fun</h2>
      </aside>
      <div class='gallery'>
        {{{body}}}
      </div>
    </main>
    <!-- If the user is logged in, load the JS script for when they logout -->
    {{#if loggedIn}}
      <script src='/js/logout.js'></script>
    {{/if}}
  </body>

</html>