{{#if posts.length}}
<ol>
    {{#each posts as |post|}}
    <li>
        <a href="#{{post.id}}">{{post.title}}</a>
        by {{post.user.username}}

        <p>{{{trimString post.body}}}...</p>
    </li>
    {{/each}}
</ol>
{{/if}}

